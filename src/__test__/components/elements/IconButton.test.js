// import React from 'react';
// import { mount } from 'enzyme';
// import IconButton from '../../../components/elements/IconButton';

// describe('Testing render <IconButton /> Component', () => {
//   let wrapperComponent;
//   beforeEach(() => {
//     wrapperComponent = mount(<IconButton />);
//   });
//   // ---Snapshot:
//   it('create snapshot', () => {
//     expect(wrapperComponent.find(IconButton)).toMatchSnapshot();
//   });
//   //  ---Rendering:
//   it('component renders correctly', () => {
//     expect(wrapperComponent.find(IconButton).length).toEqual(1);
//   });
// });
